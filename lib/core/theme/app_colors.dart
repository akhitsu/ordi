import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primary = Color(0xFF055E2C);
  static const primarySoft = Color(0xFF39B85A);
  static const background = Colors.white;
  static const textPrimary = Color(0xFF1E232C);
  static const textSecondary = Color(0xFF7B8088);
  static const textMuted = Color(0xFF90959D);
}
